public class Subtraction implements OperationExecut{
    public double execute(double a, double b) {
        System.out.println("Processing Diff operation of values: " + a + " and " + b);
        return a - b;
    }
}
